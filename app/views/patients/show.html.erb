<h1><%= @patient.name %></h1>

<% @patient.appointments.each do |a| %>
<div><%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> with:</div>
<div><%= link_to a.doctor.name, doctor_path(a.doctor) %></div>
<p></p>
<%end%>